import 'package:flutter/material.dart';

class DrugStoreContent extends StatelessWidget {
  const DrugStoreContent() : super(key: const Key('DrugStoreContent'));

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Drug Store Content'),
    );
  }
}
